export const CONFIG = {
  branding: {
    pageTitle:    'Karakteromregner',
    heading:      'Omregning av utenlandske karakterer',
    subheading:   'Beregn norsk karaktersnitt (A–F) basert på utenlandske karakterer og studiepoeng (ECTS)',
    // Optional institution name shown below the heading in the header.
    // Set to null to hide.
    institutionName: null,
  },
  disclaimer: {
    // Show the disclaimer link in the footer and allow opening the modal.
    enabled: true,
    // Automatically show the disclaimer on first visit (requires enabled: true).
    autoShow: true,
  },
};
